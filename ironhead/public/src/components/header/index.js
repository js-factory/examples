import { h } from 'preact';
import './header.scss';

const HeaderLayout = (props) => (
    <div>
        <header class="header">
            <h1>Top headlines</h1>
        </header>
    </div>
);

export default HeaderLayout;
